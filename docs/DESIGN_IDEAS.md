# 设计方案

### 关系型文件系统（多路径、多视图）

传统文件系统是树形的，每个文件都有唯一的路径。
但是，很多应用程序会在很多地方创建文件（bin、lib、share、config），删除软件的时候会产生残留。

不妨按关系型数据表的方式组织文件，每个文件都是一个 record，很多表都可以引用这个 record
每个应用启动的时候，都可以为这个应用准备单独的虚拟文件系统。
这样在APP看起来，就像运行在一个普通OS之上。
有点像Docker，每个应用跑在一个容器里面，容器有自己的虚拟文件系统。
只是这些虚拟文件系统并非完全隔离，用户可以将它们重新组合起来。
