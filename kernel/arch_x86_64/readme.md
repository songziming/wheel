# 硬件抽象层代码组织方式

底层代码无法做到模块划分清晰，必然高度耦合，有大量的交叉。
相关联的代码可能分散在多个地方。

例如 percpu-var，与内存管理相关，也和 SMP 相关，其中的 IST、中断栈又和中断处理相关。
划分内存布局时，必然需要知道其他模块需要如何使用内存。所以 mem 无法和其他模块解耦。

初始化过程 start/init.c 作为提纲，可以梳理 x86_64 各个模块。
